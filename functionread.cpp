//
// Created by gilem on 10/24/24.
//

/*
#include <iostream>
#include <stdlib.h>
#include <string.h>
#include <fstream>
#include<fstream>
using namespace std;
void lectura();

    int main (){
    lectura();
    system("pause");
    return 0;
}

void lectura() {
    ifstream archivo; //lectura de texto
        string texto;
        archivo.open("prueba.txt", ios::in); //modo lectura abrir el archivo

    if (archivo.fail()) {
        std::cout << "No se puede abrir el archivo";
        exit(1);

    }
    while (!archivo.eof()) {//mientras no sea el final del archivo no finalice la tarea
        getline(archivo,texto); //variable que guarda el texto del archivo
        std::cout << texto<<endl;
    }
    archivo.close();
    }

    //llamar un archivo "pruba.txt"

*/